import { createStyles } from '@mantine/core';

export const useCurrencyRateSpanStyles = createStyles(
	(theme, _params, getRef) => ({
		currencyText: {
			fontSize: 14,
			display: 'inline-block',
		},
	})
);
